# MoreTalk 更新日志

## 2026-09-08 更新日志（v1.8.1~v1.8.4 · 远程协助延迟优化）

### v1.8.4 · 保底上传（家属端画面不再冻结数秒）
- **新增保底上传**：真机实测该 ROM 的虚拟显示在屏幕内容完全静止时不出帧（原生 SurfaceFlinger 无合成事务 + ColorOS 省电放大），导致"有新帧才上传"策略下家属端画面冻结 3~4 秒（表现为延迟高）。现改为：无新帧时每 800ms 也把当前帧重新上传，刷新中继时间戳 → 家属端画面持续更新（X-Age 稳定 <1s），画面真实变化时事件驱动立即上传新帧。
- 看门狗改用"最近出帧 或 最近上传成功"判断通道活性：静止会话不再被误判停滞而反复重建虚拟显示（重建本身会打断采集）。

### v1.8.3 · 合成驱动悬浮窗（静止画面也驱动出帧，尽力而为）
- 远程协助运行期间放一个 1px 几乎不可见的悬浮窗，每 200ms 微调颜色/透明度，强制 SurfaceFlinger 持续合成（部分设备有效；真机上实测未解决静止不出帧，v1.8.4 的保底上传才是决定性修复，本窗口保留无副作用）。

### v1.8.2 · 看门狗风暴修复
- 看门狗停滞阈值与重建冷却 1.5s → 8s：真机虚拟显示出帧慢（静止 ~1fps 甚至更低），旧阈值把慢出帧误判为停滞，每 ~1.9s 重建一次虚拟显示，重建打断采集 1~2s，家属端延迟被拉到 3~4s。实测 30s 内重建 15 次 → 修复后 20s 内 ≤1 次。

### v1.8.1 · 紧急修复真机崩溃（硬件 JPEG 探测默认关闭）
- v1.8.0 默认启用的 `ImageFormat.JPEG` 硬件直出探测在 realme GT Neo5 SE（骁龙+ColorOS）上触发底层 native 崩溃（SurfaceFlinger 硬件 JPEG 路径 SIGSEGV，Java 层无法捕获）→ 启动远程协助数秒内进程卡退。现默认关闭（`ENABLE_HW_JPEG_PROBE=false`），一律走 RGBA 软件编码；编码线程分离、事件驱动上传、独立 50ms 指令轮询、帧率/质量自适应、keep-alive 等其余优化不受影响。

### 真机验证结果（realme GT Neo5 SE，房间 MT963361）
- v1.8.1 崩溃修复持续有效，无卡退；
- 静止画面家属端 X-Age 稳定在 ~100-950ms（原 4~8s 冻结）；
- 无重建风暴、无应用崩溃。

---

## 2026-09-07 更新日志（v1.6.4）

### 优化
- **移除独立「手电筒」桌面图标**（TorchActivity 及 LAUNCHER 入口），手电筒入口统一收敛到主界面「常用应用」瓦片，桌面不再有多余图标。
- **重绘应用内手电筒图标**：改为自含圆角的琥珀色方块 + 白色手电筒图形（Material 风格、居中），去掉瓦片灰底/灰边；开关状态直观区分——开=琥珀色亮起，关=灰色淡化（名称仍显示「手电筒（开/关）」）。

---

## 2026-09-06 更新日志（v1.5.0）

### 新增
- 公网远程协助架构升级为「手机上传帧 + 家属轮询」模式，彻底解决跨公网（家庭 CGNAT）经常断联的问题：
  - 手机端每约 300ms 把最新画面帧用 HTTP 短连接 POST 到家属 VPS 缓存（短连接单次请求/响应，规避 CGNAT 长连接被运营商/NAT 周期性重置）。
  - 家属浏览器端轮询 VPS 拉取最新帧刷新画面，点击/滑动指令 POST 到 VPS 指令队列。
  - 手机端轮询拉取指令并执行（点击/滑动）。
  - VPS 中继（relay_v2）新增接口：POST/GET /frame、POST/GET /cmd、GET /meta、控制页 /ctrl?room=XXX。
- 房间号固定持久化：首次启动生成后永久保存，家属可用固定网址访问（不再每次随机变号）。
- 设置页远程协助卡片显示固定外网网址：`http://VPS:8899/ctrl?room=固定房间号`。

### 修复
- 修复多次点击「远程协助」导致程序堆积：RemoteAssistService 启动幂等 + setupProjection 防重 + RemoteAssistActivity 已运行时直接退出。
- 修复公网长连接被重置导致画面/控制频繁断联（架构性修复，见上）。

### 优化
- 设置页新增「远程协助」管理卡片：运行状态/房间号/外网地址显示 + “停止远程协助并清理”按钮。
- 远程协助服务日志补充上传/拉取/指令执行记录，便于公网排障。

---

## 2026-09-06 更新日志

### 新增
- 设置页新增「远程协助」管理卡片：实时显示运行状态（运行中·房间号/未运行）与外网访问网址；提供“停止远程协助并清理”按钮，一键彻底停止服务并释放录屏、端口等资源。
- 公网远程协助端到端打通：手机隧道经家属 VPS 中继（房间配对 WebSocket），家属浏览器可从外网远程查看画面并点击/滑动操控（已验证 status、连续画面帧、tap 注入）。

### 修复
- 修复多次点击「远程协助」导致程序堆积：RemoteAssistService 启动改为幂等，服务已在运行时重复启动请求直接忽略；RemoteAssistActivity 已运行时直接退出，不再重复请求录屏授权；setupProjection 增加防重复初始化保护。

### 优化
- 远程协助服务日志补充隧道收到的指令与回发内容、断开原因，便于公网排障。

---

## 2026-09-05 更新日志

### 新增
- 新增独立「手电筒」桌面图标：点击即开关手电筒并自动返回，无需进入应用（无闪光灯机型自动提示）。
- 常用应用支持添加「手电筒」快捷入口，首页点击图标直接开关手电筒。
- 常用应用图标放大至与桌面悬浮球同尺寸（76dp），更易点按。
- 新增「桌面悬浮球」：屏幕右边缘固定悬浮按钮（居中偏上，不可拖动），点一下即可从任意界面回到 MoreTalk 主界面，可在设置页开关；20 秒无点击自动淡化为半透明，点击恢复并重新计时。
- 设置页新增「桌面悬浮球」开关（默认开启，需悬浮窗权限）。
- 新增「微信消息点读」：收到微信新消息自动语音播报发送者和内容（聊天页检测 + 通知兜底，自动去重防重复朗读；需开启无障碍服务；默认关闭，隐私优先）。
- 常用应用网格内新增「微信点读」开关瓦片：绿色=开、灰色=关，点击直接切换；开启后在微信里点按任意消息即可朗读该消息内容。

### 修复
- 移除 Manifest 中未实现的历史遗留 WeChatActivity 声明。

### 优化
- 顶部状态区调整：星期几移到日期下一行，右侧显示实时时间（含秒，每秒刷新）；星期与时间统一字号样式对齐；右下角设置按钮缩小，降低老人误触概率。

---

## 2026-05-02 更新日志

### 新增
- 联系人点击逻辑优化：新增“单一通话方式直接拨打”设置项，可由用户自行决定开启或关闭。
- 首页电量展示增强：支持按电量区间显示不同颜色图标与百分比，并可以在“显示电量”和原先的天气细节信息之间切换。
- 低电量提醒完善：支持独立开关，并按电量阈值进行语音提醒；提醒音量走全局播报音量。
- 播报设置调整：天气播报改为开关控制；新增全局“播报音量”滑条，用于统一控制天气、低电量、联系人播报。

### 优化
- 电量状态文案优化：统一了充电中、低电量、建议充电、正常电量、已充满等显示文案。
- 日期大小优化：根据屏幕宽度自适应字体大小，防止窄屏手机日期溢出屏幕。
- 微信联系人播报优化：微信语音/视频播报时优先读联系人姓名，微信备注仅用于后台查找微信联系人。
- 常用应用数量上限调整：最大可选应用数量由 6 提升到 8。
- README 资源本地化整理：部分外链图片改为本地资源，并补充/调整了更新日志入口与展示内容。

### 修复
- 修复进入聊天界面后找不到“+”，手动返回重新进入聊天界面后才生效的问题。
- 修复设置为默认桌面后，返回键可以循环点击的问题。
- 首页布局适配优化：修复选择 8 个常用应用后联系人区域被挤出、无法点击的问题，现在首页支持纵向滚动，联系人可正常触达。

---

## 2026-04-21 更新日志

### 新增
- 接入内置 Matcha TTS，并补充 sherpa-onnx 运行依赖与本地模型资源。
- 新增内置语音服务层，支持模型检测、PCM 播放与失败原因回传。
- 设置页新增语音引擎选项，支持 `自动 / 系统 TTS / 内置 Matcha` 三种模式。

### 优化
- 自动语音引擎优先级调整为 `内置 Matcha > 系统 TTS`。
- 应用启动时预加载内置语音引擎，减少首次播报等待时间。
- 当前实际引擎与内置模型状态统一放到设置页展示，不再显示在首页天气卡片上。
- 优化天气播报文案：
  - 年份按中文逐位播报。
  - 农历年份继续使用 `getYearInChinese()`，并将 `〇` 统一替换为 `零`。
  - 风向改为“东风 / 北风 / 东北风”等更自然的说法。
- 优化联系人、微信备注等名称中的数字读法，避免直接播报阿拉伯数字。
- 无障碍自动执行流程的每一步间隔统一翻倍，并且最低不低于 `1000ms`。

### 修复
- 修复设置页切换语音引擎后状态不立即刷新的问题。
- 修复内置 TTS 已完成合成但没有声音的播放问题。
- 修复天气中的湿度、日期等数字在内置语音下播报不自然的问题。

---

## 2026-04-19 更新日志

### 新增
- 新增“设为默认桌面”功能，支持将 MoreTalk 设置为系统默认桌面应用。
- 新增“取消默认桌面”功能，可从设置页快速引导用户切换到其他桌面应用。

### 优化
- 默认桌面相关按钮改为中文显示，交互状态更清晰。
- 设置页会根据当前状态自动更新默认桌面按钮的文案与可用状态。
- “播报语速”区域的标题和倍率文字不再跟随图标大小缩放，显示效果与天气播报、联系人等模块保持一致。
- 为首页、设置页、联系人页、联系人详情页、添加联系人页、常用应用页等主要页面补充底部留白，避免设为默认桌面后内容被底部导航栏遮挡。

### 修复
- 修复天气播报可能没有声音的问题。
- 统一天气播报开关的默认值逻辑，避免设置页显示已开启但首页实际不播报。

### 涉及页面
- 首页
- 设置页
- 联系人页
- 联系人详情页
- 添加联系人页
- 常用应用页
